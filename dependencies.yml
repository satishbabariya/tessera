PACKAGE_NAME: tessera
VERSION: 14.14.0
# Tessera: minimum acceptable OpenSSL supplied by the environment. Formerly
# OPENSSL_VERSION, which pinned a prebuilt tarball fetched from static.realm.io.
OPENSSL_MIN_VERSION: 3.0.0
